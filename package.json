{
  "dependencies": {},
  "scripts": {
    "genkey": "openssl genpkey -algorithm ED25519 | tee >(openssl pkey -pubout) | pbcopy && echo 'Keypair copied to clipboard!'",
    "quickstart": "bun run quickstart.ts"
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  }
}